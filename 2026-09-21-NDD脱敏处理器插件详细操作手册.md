# 2026-09-21 NDD 脱敏处理器插件详细操作手册

> 适用版本：脱敏处理器 v1.0（2026-09-21）
> 适用编辑器：Notepad--（NDD）Windows 64 位 Qt5 版，建议 v3.8.2 及以上
> 工程目录：`C:\dev\code\ndd_plugins\desensitive_processor`（WSL：`/mnt/c/dev/code/ndd_plugins/desensitive_processor`）

---

## 一、项目概述

### 1.1 这是什么

NDD（Notepad--）的脱敏插件。用户配置一张「关键词映射表」（如 `abc -> aldjfakldfj`），
插件即可对当前编辑的文档一键执行：

- **脱敏**：把文档中所有「原文」替换为「替换为」
- **还原**：按映射反向替换，恢复成原文

典型用途：把测试数据、客户信息、内部代号脱敏后再发给外部；收到脱敏文档后可反向还原。

### 1.2 功能清单

| 功能 | 说明 |
| --- | --- |
| 映射表 | 启用开关 / 原文 / 替换为，支持增删、上移下移 |
| 脱敏 | 全文替换（可改为仅选区） |
| 还原 | 反向替换 |
| 单遍扫描 | 一次扫描完成全部替换，最长匹配优先，不会「a→b、b→c」连锁误替换 |
| 区分大小写 | 默认开启；关闭后 ABC/abc 都命中 |
| 仅处理选中文本 | 默认关闭（全文）；开启后只替换选中的部分 |
| 生成随机串 | 为当前行生成 12 位随机替换串（自动查重） |
| 批量粘贴 | 一次粘贴多行 `abc->xxx`（支持 `->`、`=>`、Tab、逗号、`=` 分隔） |
| 导入/导出 | JSON / CSV / 纯文本三种格式 |
| 撤销 | 每次操作可用 Ctrl+Z 一步撤销 |
| 配置持久化 | 自动保存，重启 NDD 后映射仍在 |
| 冲突校验 | 原文重复、替换文重复会阻止执行；替换文与其它原文冲突会提醒 |

### 1.3 交付物

| 文件 | 说明 |
| --- | --- |
| `build\out\desensitive_processor.dll` | 插件本体（编译产物） |
| `dist\desensitive_processor-v1.0.zip` | 可分发安装包（含 DLL、安装/卸载脚本、说明） |
| `2026-09-21-NDD脱敏处理器插件详细操作手册.md` | 本手册 |

---

## 二、安装手册

### 2.1 用发布包安装（推荐，其他电脑）

1. 找到发布包：`dist\desensitive_processor-v1.0.zip`
2. 拷贝到目标电脑并解压，目录内包含：
   - `desensitive_processor.dll`（插件）
   - `install.cmd` / `install.ps1`（一键安装）
   - `uninstall.cmd` / `uninstall.ps1`（一键卸载）
   - `安装说明.txt`（简版说明）
3. 双击 `install.cmd`：
   - 自动从注册表和常见路径查找 Notepad--
   - 找不到时会弹出文件选择框，手动选择 `Notepad--.exe`
   - 目标目录无写权限时自动请求 UAC 提权，点「是」
   - 若检测到目标目录有 `Qt6Core.dll`（Qt6 版本 NDD），会提示不兼容并退出
4. 重启 Notepad--，菜单栏 →「插件」→「脱敏处理器」

### 2.2 手动安装（不用脚本）

1. 把 `desensitive_processor.dll` 复制到 NDD 安装目录的 `plugin` 文件夹：
   - 安装版：`C:\Program Files\Notepad--\plugin\`
   - 绿色版：解压目录 `...\Notepad--\plugin\`
2. 重启 NDD 即可。

> 提示：C 盘 Program Files 需要管理员权限，最简单的做法是复制时点「继续」授权。

### 2.3 安装到本机正式版（开发机快捷方式）

工程根目录已备好两个脚本：

- `install-to-ndd.cmd`：自动把最新编译产物复制到 `C:\Program Files\Notepad--\plugin`（弹 UAC）
- `uninstall-from-ndd.cmd`：从正式版卸载

### 2.4 卸载

- 发布包：双击 `uninstall.cmd`
- 手动：删除 `plugin` 目录下的 `desensitive_processor.dll`，重启 NDD
- 映射配置保留在 `%APPDATA%\ndd-desensitive\mappings.json`，如需彻底清除可删整个
  `%APPDATA%\ndd-desensitive` 目录

### 2.5 兼容性说明

| 环境 | 是否支持 |
| --- | --- |
| Windows 64 位 NDD v3.x（Qt 5.15.2 构建，如 v3.8.2） | 支持 |
| NDD 目录存在 `Qt6Core.dll`（Qt6 版本） | 不支持，需在 Qt6 环境重新编译 |
| 32 位 NDD | 不支持 |
| Linux / macOS 版 NDD | 本 DLL 不支持，需在对应平台用 Qt5 重新编译 `.so` / `.dylib` |

---

## 三、使用手册

### 3.1 打开插件

NDD 菜单栏 →「插件」→「脱敏处理器」，弹出配置窗口（非模态，可一直开着）。

### 3.2 界面说明

```
┌──────────────────────────────────────────────────────────────┐
│ 添加行 删除行 上移 下移 生成随机串 批量粘贴 导入 导出        │
├──────────────────────────────────────────────────────────────┤
│ 启用 │ 原文            │ 替换为                              │
│  ☑   │ 张三            │ 张*                                 │
│  ☑   │ 13800138000     │ 138****8000                         │
├──────────────────────────────────────────────────────────────┤
│ ☑ 区分大小写   ☐ 仅处理选中文本            打开配置目录      │
│ [脱敏] [还原] [保存配置]  状态：……                           │
└──────────────────────────────────────────────────────────────┘
```

- **启用**：勾选的行才参与替换；取消勾选可临时停用某条映射
- **原文 / 替换为**：双击单元格编辑；「替换为」可以为空（表示删除原文）
- **上移/下移**：调整顺序（算法按最长匹配优先，顺序一般不影响结果，主要用于人工查看）

### 3.3 基本操作流程

1. 点「添加行」，在「原文」填 `abc`，在「替换为」填 `aldjfakldfj`（或在「替换为」单元格点「生成随机串」自动生成）
2. 在 NDD 中打开待处理文档，点插件窗口的「脱敏」
3. 弹出「脱敏完成：共替换 N 处，命中 M 个关键词（可 Ctrl+Z 撤销）」
4. 检查文档无误后 Ctrl+S 保存；如需反悔，Ctrl+Z 一步还原
5. 点「还原」可把文档恢复成脱敏前的原文（需保证映射表未变）

### 3.4 批量粘贴

点击「批量粘贴」，每行一条映射，支持以下写法（示例）：

```
张三->张*
13800138000=>138****8000
110101199003077777,110101********7777
abc	aldjfakldfj
# 以 # 或 // 开头的行是注释，会被忽略
```

- 分隔符自动识别：`->`、`=>`、Tab、英文逗号、`=`、空白
- 解析成功的行追加到表格；无效行会被跳过并在状态栏显示数量
- 若粘贴的映射已存在同原文行，会新增一行；建议导入前先清理重复

### 3.5 导入 / 导出

- **导出**：把当前映射表保存为 JSON / CSV / 文本，用于备份或跨电脑迁移
- **导入**：从文件读取映射并追加到表格；支持：
  - JSON：`[{"enabled":true,"from":"abc","to":"xxx"}]`
  - CSV：首行表头 `enabled,from,to`（引号包裹的单元格支持）
  - 文本：与「批量粘贴」相同格式

典型迁移流程：旧电脑「导出」→ 拷文件 → 新电脑插件窗口「导入」。

### 3.6 仅处理选中文本

勾选后：
- 若编辑器里有选中内容，只对选区替换，选区外不动
- 若没有选中内容，则等同于全文处理

### 3.7 区分大小写

- 勾选（默认）：`abc` 只匹配 `abc`，不匹配 `ABC`
- 不勾选：`abc`、`AbC`、`ABC` 都匹配，统一替换为同一替换串

### 3.8 撤销

每次脱敏/还原都是一个撤销步骤，直接按 Ctrl+Z 即可整体撤销，不需要逐步撤销。

### 3.9 注意事项

- 还原依赖「当前映射表」。如果脱敏后修改了映射表（例如改了替换串），可能无法完整还原
- 如果替换串与其它映射的原文冲突，插件会弹提醒（可选择继续），建议避免这种映射
- 大文件快速模式（只读）下无法替换，插件会提示
- 单次处理文本上限 30M 字符（超过会提示），避免卡顿

---

## 四、配置与数据

### 4.1 映射配置文件

- 路径：`%APPDATA%\ndd-desensitive\mappings.json`
  例如 `C:\Users\qxzha\AppData\Roaming\ndd-desensitive\mappings.json`
- 格式：

```json
[
    {
        "enabled": true,
        "from": "abc",
        "to": "lvurb23r7zyd"
    }
]
```

- 表格任何改动都会自动保存；关闭插件窗口时也会保存
- 想手工编辑该文件也可以（改完重开插件窗口生效）

### 4.2 诊断日志

- 路径：`%APPDATA%\ndd-desensitive\plugin.log`
- 每次点击脱敏/还原都会记录：映射数量、编辑器指针、命中的关键词/处数、写回结果
- 排障时优先看这个文件；日志会不断追加，可定期清空

### 4.3 界面上的「打开配置目录」

一键用资源管理器打开 `%APPDATA%\ndd-desensitive`，方便查看配置和日志。

---

## 五、技术原理（维护/二次开发必读）

### 5.1 NDD 插件机制

- NDD 启动时扫描 `<NDD安装目录>\plugin\*.dll`，用 `QLibrary` 加载
- 插件必须导出两个 C 接口：
  - `bool NDD_PROC_IDENTIFY(NDD_PROC_DATA*)`：返回插件名、说明、版本、作者、菜单类型
  - `int NDD_PROC_MAIN(...)`：主程序把主窗口指针、当前编辑器获取函数、主程序回调传入
- `m_menuType = 0`：插件在「插件」菜单下生成一个一级菜单项；点击触发插件内部回调
- 插件接口版本为官方 2.0 接口；本插件按 NDD v3.8.2 的头文件编写
- 关键类型声明见 `sdk\include\pluginGl.h`，官方示例参考 `sdk\reference_plugin.h`

### 5.2 与 NDD 的 ABI 绑定

| 依赖 | 取值 | 原因 |
| --- | --- | --- |
| Qt 版本 | 5.15.2（msvc2019_64） | 与 NDD v3.8.2 内置 Qt5Core.dll 版本一致 |
| 编译器 | MSVC（v143 实测可用，官方为 v142） | MSVC 2015-2022 ABI 向后兼容 |
| 平台 | Windows x64 | 与 NDD 一致 |
| 编辑器库 | `qmyedit_qt5.dll`（NDD 定制的 QScintilla 5.15.2） | 插件通过 `sdk\lib\qmyedit_qt5.lib` 链接 |

> 结论：NDD 升级到 Qt6 版本后，插件必须用 Qt6 重新编译；换 NDD 大版本时，
> 建议用 `tools\gen_qmyedit_lib.cmd` 重新从目标 NDD 的 `qmyedit_qt5.dll`
> 生成 import lib 再编译，避免符号不匹配。

### 5.3 编辑器读写

- 通过 `NDD_PROC_MAIN` 传入的 `getCurEdit(QWidget*)` 获取当前编辑框 `QsciScintilla*`
- 读取全文：`editor->text()`；读取选区：`editor->selectedText()`
- **写回全文/选区**：不能使用 `replaceSelectedText()` / `setText()`（见 5.4），
  实际使用底层 Scintilla 消息：

```cpp
const QByteArray utf8 = result.toUtf8();
editor->beginUndoAction();
editor->SendScintilla(SCI_SELECTALL);          // 全文模式才需要；选区模式保留原选区
editor->SendScintilla(SCI_REPLACESEL, utf8.constData());
editor->endUndoAction();
```

- `beginUndoAction/endUndoAction` 保证整个替换是一个撤销步骤
- 替换完成后会回读文本校验；万一未来 NDD 版本写回失效，会回退到 `setText()`
  并在界面提示「写入编辑器失败」

### 5.4 已知坑：NDD 定制版 QScintilla 的写回问题

NDD 对 QScintilla 做了定制（`qmyedit_qt5.dll`），实测：

- `QsciScintilla::replaceSelectedText()` 内部依赖的选中状态标志不可靠，
  调用后文档毫无变化（`text() == 原文`）
- `QsciScintilla::setText()` 走 NDD 自定义消息 `SCI_SET_UTF8_TEXT(6001)`，
  在该场景下曾把整个文档清空（替换内容也没写进去）
- **解决办法**：绕开这两个高层封装，直接发 `SCI_SELECTALL` + `SCI_REPLACESEL`，
  并传入 UTF-8 字节；该路径在 NDD v3.8.2 上验证正常

定位过程复现命令：
- `tools\scintilla_test\`：独立验证 QScintilla 的写回行为（selectAll+replace 正常）
- 插件日志里逐步记录 try1/try2/try3 结果（历史版本），最终确认 try2 生效

### 5.5 替换算法

`src\desensitizer.cpp`：

1. 收集所有启用且原文非空的行；还原时把 `to` 当原文、`from` 当替换文
2. 按 key 长度降序排序（保证最长匹配优先）
3. 用 `QRegularExpression::escape` 转义后拼成 `key1|key2|...` 单条正则
4. `globalMatch` 单遍扫描原文，一边扫描一边拼接结果，替换结果不会再被扫描
5. 命中统计按映射行索引累加，用于界面提示

特性：
- 不会级联：`a→b`、`b→c` 同时存在时，原文 `a b` 得到 `b c`（而不是 `c c`）
- 大小写：关闭区分时对捕获文本做 `toLower()` 后查表
- 校验：原文重复/替换文重复 = 阻断错误；替换文与其他原文冲突 = 警告
  （`Desensitizer::validate()`）

---

## 六、开发环境搭建（从零开始）

### 6.1 一次性安装

| 组件 | 安装方式 | 说明 |
| --- | --- | --- |
| VS 2022 Build Tools | 运行 `build\install_vs.cmd` 或 `winget install --id Microsoft.VisualStudio.2022.BuildTools --override "--quiet --wait --add Microsoft.VisualStudio.Workload.VCTools --includeRecommended"` | 需要 Desktop C++ 工作负载（MSVC + Windows SDK） |
| Qt 5.15.2 msvc2019_64 | 见 6.2 | 官方开源离线安装包 5.15 起已下架，本机采用「在线仓库 qtbase 归档包手动解压」方案 |

### 6.2 本机 Qt 5.15.2 的安装方法（已执行）

1. 从镜像下载 qtbase 包（34MB）：
   `https://mirrors.ustc.edu.cn/qtproject/online/qtsdkrepository/windows_x86/desktop/qt5_5152/qt.qt5.5152.win64_msvc2019_64/5.15.2-0-202011130602qtbase-Windows-Windows_10-MSVC2019-Windows-Windows_10-X86_64.7z`
   （校验 sha256：`e563de40230295841c2eece26e347de709edc4bf035515cc239cae3c994c9af6`）
   > 清华镜像该文件曾损坏，USTC 镜像正常
2. 用 7-Zip 解压到 `C:\Qt`，得到 `C:\Qt\5.15.2\msvc2019_64`
3. 该归档包 `qconfig.pri` 标记为 `QT_EDITION = Enterprise`，qmake 会做 license 检查；
   本机已改为 `OpenSource` 并清空 `QT_LICHECK`（文件：
   `C:\Qt\5.15.2\msvc2019_64\mkspecs\qconfig.pri`，原文件备份为 `qconfig.pri.enterprise.bak`）
4. 校验工具链：应有 `bin\qmake.exe`、`bin\moc.exe`、`include\`、`lib\`

> 正式分发/合规建议：使用官方在线安装器（开源账号）安装 Qt，避免使用 Enterprise 归档包。

### 6.3 NDD 插件 SDK（sdk 目录）

已从 NDD v3.8.2 源码中提取并放在工程里，无需重复下载：

```
sdk/
├── include/pluginGl.h              插件接口头文件
├── qscint/src/Qsci/*.h             QScintilla 头文件（67 个，v3.8.2 原版）
├── lib/qmyedit_qt5.lib             从本机 NDD 的 qmyedit_qt5.dll 生成的导入库
└── reference_plugin.h              NDD 主程序 plugin.h 参考（NDD_PROC_MAIN 签名）
```

### 6.4 生成 qmyedit_qt5.lib（换 NDD 版本时必做）

```
tools\gen_qmyedit_lib.cmd
```

原理：`dumpbin /exports "C:\Program Files\Notepad--\qmyedit_qt5.dll"` 导出符号表，
`tools\make_def.py` 转成 `.def`，再用 `lib /def` 生成 `.lib`，输出到 `sdk\lib\`。
这样导入库与目标 NDD 的 DLL 100% 匹配（本机从 NDD v3.8.2 导出 2403 个符号）。

---

## 七、编译、测试与打包

### 7.1 编译并部署到测试版

```
build.cmd
```

流程：vcvars64 → qmake → nmake → 复制 DLL 到
`test\Notepad--v3.8.2-win10-portable\plugin\`。

产物：`build\out\desensitive_processor.dll`。

### 7.2 测试工具

| 工具 | 命令 | 内容 |
| --- | --- | --- |
| 引擎单元测试 | `tools\engine_test\build_run.cmd` | 16 项：简单替换、最长匹配、防级联、大小写、中文、正反向、命中统计、停用行、重复校验、冲突警告 |
| 插件加载烟雾测试 | `tools\load_test\build_run.cmd` | LoadLibrary + NDD_PROC_IDENTIFY，校验导出与依赖（含 qmyedit/Qt DLL） |
| QScintilla 写回验证 | `tools\scintilla_test\build_run.cmd` | 独立验证 selectAll/replaceSelectedText/SCI_REPLACESEL 的行为 |

### 7.3 手工验收清单

1. NDD 菜单出现「插件 → 脱敏处理器」
2. 加映射 `abc->xxx`，点「脱敏」→ 全文替换、弹出统计
3. Ctrl+Z → 一步恢复；再脱敏；点「还原」→ 回到原文
4. 勾选「仅处理选中文本」→ 只改选区
5. 取消「区分大小写」→ `ABC` 也命中
6. 关闭插件再打开、重启 NDD → 映射仍在
7. 导出 JSON → 清空表格 → 导入 → 恢复
8. 大文件快速模式（只读）→ 提示无法替换

### 7.4 打包发布

```
package.cmd
```

生成 `dist\desensitive_processor-v1.0.zip`（DLL + 安装/卸载脚本 + 安装说明）。
版本号在 `package.cmd` 的 `set NAME=` 里修改。

---

## 八、故障排查

| 现象 | 原因/处理 |
| --- | --- |
| 「插件」菜单里没有「脱敏处理器」 | DLL 没放进 NDD 的 `plugin` 目录；或放错目录（是 exe 同级的 plugin）；或 NDD 是便携版不支持插件（官方便携版支持）；重启 NDD 再看 |
| 点击菜单没窗口 | 检查 `%APPDATA%\ndd-desensitive\plugin.log`，看是否记录 clicked |
| 点「脱敏」提示「未发现可脱敏的内容」 | 映射行未勾选启用；原文与文档不一致；大小写开关不合适；选区模式勾了但没选内容 |
| 点「脱敏」提示「写入编辑器失败」 | NDD 版本与插件 ABI 不匹配，或该文档是只读/特殊模式；把 plugin.log 发给开发者 |
| 提示「映射列表为空」 | 表格里没有任何有效行（原文为空的会被忽略） |
| 提示原文/替换文重复 | 修正重复项，或在「区分大小写」关闭时不区分大小写地重复 |
| 重启后映射丢失 | 检查 `%APPDATA%\ndd-desensitive\mappings.json` 是否可写 |
| NDD 启动崩溃/插件加载失败导致异常 | 把 `plugin\desensitive_processor.dll` 改名或删除后重启 |
| 换电脑安装后无菜单 | 对方 NDD 可能是 Qt6/32 位版本，见 2.5 |
| Qt6 版 NDD | 本插件不通用，需要用 Qt6 + 该版本 NDD SDK 重新编译（可联系开发者） |

日志位置：`%APPDATA%\ndd-desensitive\plugin.log`
配置位置：`%APPDATA%\ndd-desensitive\mappings.json`

---

## 九、目录结构与关键路径速查

```
C:\dev\code\ndd_plugins\desensitive_processor\
├── src\                       插件源码
│   ├── plugin_export.cpp      NDD 导出接口（IDENTIFY/MAIN）
│   ├── instanceobj.h/.cpp     插件实例与窗口生命周期（单实例复用）
│   ├── mainwindow.h/.cpp      配置界面 + 操作回写 + 日志
│   ├── desensitizer.h/.cpp    替换引擎（单遍扫描、校验）
│   └── mappingstore.h/.cpp    配置读写、导入导出、批量文本解析
├── sdk\                       插件 SDK（头文件 + import lib）
├── tools\
│   ├── gen_qmyedit_lib.cmd    生成 qmyedit_qt5.lib
│   ├── make_def.py            dumpbin 导出表 → .def
│   ├── engine_test\           引擎单元测试
│   ├── load_test\             插件加载烟雾测试
│   └── scintilla_test\        QScintilla 写回机制验证
├── test\
│   ├── Notepad--v3.8.2-win10-portable\   绿色版 NDD 测试环境
│   ├── sample.txt             测试样例（含 abc/中文/电话/身份证）
│   └── start-test-ndd.cmd     启动测试版 NDD 并打开样例
├── dist\                      发布包输出
├── dist-template\             发布包模板（install/uninstall/说明）
├── build.cmd                  编译 + 部署到测试版
├── package.cmd                打包发布 zip
├── install-to-ndd.cmd         安装到本机正式版 NDD
├── uninstall-from-ndd.cmd     从本机正式版卸载
├── desensitive_processor.pro  qmake 工程文件
└── README.md                  快速上手说明
```

其他关键路径：

| 用途 | 路径 |
| --- | --- |
| NDD 正式版安装目录 | `C:\Program Files\Notepad--` |
| NDD 正式版插件目录 | `C:\Program Files\Notepad--\plugin` |
| NDD 内置 Qt | `C:\Program Files\Notepad--\Qt5*.dll`（5.15.2） |
| NDD 定制编辑器库 | `C:\Program Files\Notepad--\qmyedit_qt5.dll` |
| 本机 Qt SDK | `C:\Qt\5.15.2\msvc2019_64` |
| VS Build Tools | `C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools` |
| 插件配置 | `%APPDATA%\ndd-desensitive\mappings.json` |
| 插件日志 | `%APPDATA%\ndd-desensitive\plugin.log` |
| NDD 自身配置 | `%APPDATA%\notepad\`（nddsets.ini 等） |

---

## 十、版本记录

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| v1.0 | 2026-09-21 | 首个版本：映射表格、脱敏/还原、随机串、批量粘贴、导入导出、大小写/选区开关、撤销、配置持久化；解决 NDD 定制 QScintilla 写回问题 |

---

## 十一、常用命令速查

```bat
:: 编译插件并部署到测试版
build.cmd

:: 运行引擎单元测试（应输出 ALL TESTS PASSED）
tools\engine_test\build_run.cmd

:: 打包发布
package.cmd

:: 安装到本机正式版 NDD（管理员）
install-to-ndd.cmd

:: 从正式版卸载
uninstall-from-ndd.cmd

:: 重新生成 qmyedit_qt5.lib（换 NDD 版本时）
tools\gen_qmyedit_lib.cmd

:: 启动测试版 NDD（自动打开样例文件）
test\start-test-ndd.cmd
```
